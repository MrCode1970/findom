personality = "friendly"
model = "gpt-5.3-codex"
model_reasoning_effort = "high"
model_verbosity = "low"

# Safe defaults for routine work in this repository.
approval_policy = "on-request"
sandbox_mode = "workspace-write"

# Enable web search only when explicitly needed by task.
# web_search = "disabled"
